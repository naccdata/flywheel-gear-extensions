# Execution Log

Tracks what each subagent did, which quality checks it ran, and issues found and
fixed during task execution. Keeps execution detail out of the main agent
context.

| Task | Quality Checks Run | Notes |
|------|--------------------|-------|
| 1.1 | pants_fix (file), pants_check (dir: common/src/python/authorization) | Reworked `ResourceObject` in `authorization/models.py`: removed `id`, added required `label` (min_length=1), `flat_id` aliased to `flatId` (read-only, default None), kept `type`/`name` and optional parents `study`/`center`/`community`; set `model_config = ConfigDict(populate_by_name=True, extra="ignore")`; added `request_dump()` (by_alias, exclude_none, exclude flat_id). Did NOT add `check_parent_fields` (task 1.2). fix ✓, check ✓. No pre-existing errors surfaced in the authorization directory. |
| 1.2 | pants_fix (file), pants_check (dir: common/src/python/authorization) | Added module-level `_ORGANIZATION_TYPES = frozenset({"study", "research_center", "community"})` (org types recognized from seed-path `parent_type` values in `projects/hierarchy_seeder.py`). Added `@model_validator(mode="after") check_parent_fields` to `ResourceObject` enforcing the per-type combination table: organization type → no parents (raises naming the type); `data_pipeline` → (study+center) or (study); `dashboard` → (study+center) or (study) or (community); `page` → (study+center) or (study) or (center) or (community). Unknown types (not org, not in the table) are left unconstrained beyond the label rule. Violations raise `ValueError` naming the offending type. fix ✓, check ✓. No new errors in the authorization directory. |
| 1.3 | pants_fix (file), pants_test (file: common/test/python/authorization_test/test_property_models.py) | Extended existing `test_property_models.py` with Property 7 (per-type parent-field validation). Added `TestProperty7ParentFieldCombinations` with two Hypothesis-driven methods: `test_construction_succeeds_iff_combination_permitted` generates organization + constrained resource types (`data_pipeline`/`dashboard`/`page`) crossed with every (study, center, community) presence tuple and asserts `ResourceObject` construction succeeds iff the combination is in a test-local allowed-combination table (mirroring the design's per-type table), else raises `ValidationError` whose message names the offending type; `test_unknown_types_are_unconstrained` asserts forward-compatible types outside the table accept any parent combination. Added `ResourceObject` to the model imports. Tests exercise only the public construction interface. fix ✓, test ✓ (all 1892 tests passed, +2 new). Not a PBT-status task (regular property test, not in a PBT-tracked list). |
| 2.1 | pants_fix (file), pants_check (dir: common/src/python/authorization) | Migrated the four write-request models in `authorization/models.py` to structured `resource`. `GrantRequest`, `RevokeRequest`, `PermissionCheckRequest`: now plain `BaseModel`s with `user_id`/`relation`/`resource: ResourceObject` and a `request_body()` emitting `{userId, relation, resource: resource.request_dump()}` (JSON-encoded bytes). `BatchOperationModel`: added `resource: ResourceObject`, kept `action`/`user_id`/`relation`. Removed legacy top-level `type`/`resourceId` from all four. Removed `_ResourceObjectValidatorMixin` entirely (its only consumers were these write models; no non-write model used it) and the now-unused `_MIN/_MAX_RESOURCE_OBJECT_LENGTH` constants. Added `import json`. Added `GrantRequest/RevokeRequest/BatchOperationModel.model_rebuild()` after `ResourceObject` is defined to resolve the forward references (those models precede `ResourceObject` in the module). fix ✓. check: models.py clean; 4 expected errors remain in `client.py` (lines 148/246/388 + PermissionCheckRequest) for the old `type`/`resource_id` call sites — these are addressed in tasks 2.3/2.4 per plan, not fixed here. No unrelated pre-existing errors surfaced. |
| 2.2 | pants_fix (file), pants_check (dir: common/src/python/authorization) | Migrated the `BatchOperation` caller-facing domain type in `authorization/models.py` to structured fields matching the design target shape. Added `resource_label: str`, `relation: str`, and optional `center: str \| None = None`, `study: str \| None = None`, `community: str \| None = None`; kept `action`, `user_id`, `resource_type`. Removed the flat `resource_id` field (replaced by structured fields). In-place edit; no new modules/renames; client.py untouched (task 2.4). fix ✓. check: models.py clean; 5 expected errors remain in `client.py` (lines 148/246/388 "Missing named argument resource" for GrantRequest/RevokeRequest/BatchOperationModel + 2 more) — these are the old call sites addressed in tasks 2.3/2.4 per plan, not fixed here. No unrelated pre-existing errors surfaced in the authorization directory. |
| 2.3 | pants_fix (file: common/src/python/authorization/client.py), pants_check (dir: common/src/python/authorization) | Migrated `grant`/`revoke`/`check_permission` request construction in `client.py` to structured `resource`. Changed signatures from `(user_id, resource_type, resource_id, relation)` to `(user_id, resource_type, resource_label, relation, center=None, study=None, community=None)` (discrete parent params per design "client.py" section). Added a shared `@staticmethod _build_resource(...)` helper that constructs a `ResourceObject` and, on failure (missing type, empty label, or invalid parent-field combination), catches pydantic `ValidationError` (imported as `PydanticValidationError`) and re-raises the client's `ValidationError` naming the offending type/label before any HTTP call (Req 1.7). Each method now builds the request with `resource=resource` and serializes via `request.request_body()` (no top-level `type`/`resourceId`, no `flat_id`). Updated the log lines to report `label` instead of `resource_id`. Added `ResourceObject` to the models import. Left the idempotent 409 (grant) / 404 (revoke) `GrantResult`/`RevokeResult` construction to task 2.4: for now they pass `resource=resource` alongside the still-required `type`/`resource_id=resource_label` so grant/revoke type-check; the response-model field migration is task 5.1/2.4. Did NOT touch `_execute_batch_chunk`, `retry_on_503`, `_BATCH_CHUNK_SIZE`, or `_IDEMPOTENT_ERROR_CODES`. fix ✓. check: 2 errors remain, both in `_execute_batch_chunk` (line 478 `BatchOperationModel` missing `resource`; line 483 `BatchOperation` has no `resource_id`) — expected, owned by task 2.4 (batch path not yet migrated). No unrelated pre-existing errors surfaced. |
| 2.4 | pants_fix (file: common/src/python/authorization/client.py), pants_check (dir: common/src/python/authorization) | Migrated batch request construction and idempotent 409/404 result construction in `client.py`. `_execute_batch_chunk` now builds each `BatchOperationModel` with `resource=self._build_resource(resource_type=op.resource_type, resource_label=op.resource_label, center=op.center, study=op.study, community=op.community)` from the structured `BatchOperation` fields (reusing the `_build_resource` helper added in 2.3), replacing the removed `type=op.resource_type`/`resource_id=op.resource_id`. The idempotent 409-on-grant `GrantResult` and 404-on-revoke `RevokeResult` now derive identity from the request's structured `ResourceObject`: `type=resource.type` and `resource_id=resource.flat_id or resource.label` (flat_id is None on a freshly-built request resource, so this resolves to the structured label — identity comes from the ResourceObject, not a caller-supplied flat id), with `resource=resource` attached. `GrantResult`/`RevokeResult` still carry `type`/`resource_id` at this point (response-model migration is task 5.1), so both are populated from the structured resource to type-check cleanly. Left `_BATCH_CHUNK_SIZE`, `retry_on_503`, `_IDEMPOTENT_ERROR_CODES` unchanged; chunking order preservation and per-op conflict/not_found classification (`_classify_batch_errors`) untouched. fix ✓, check ✓ (authorization directory now type-checks cleanly — the 2 errors previously owned by 2.4 are resolved). No unrelated pre-existing errors surfaced. |
| 2.5 | pants_fix (file), pants_test (file: common/test/python/authorization_test/test_property_grant_revoke.py) | Added Property 1 (Requests carry structured identity and never flat identity) to `test_property_grant_revoke.py`. New class `TestMigrationProperty1StructuredIdentity` with four Hypothesis methods covering `GrantRequest`, `RevokeRequest`, `PermissionCheckRequest` (via `request_body()`), and `BatchOperationModel` (serialized via `resource.request_dump()`, since it has no `request_body()`). Added a `structured_identities()` composite strategy that draws a resource type and a permitted (study, center, community) presence combination per the design's per-type table, a non-empty label, and ~50% of the time an arbitrary server-owned `flat_id` (as a parsed response resource would carry) — so construction always succeeds and the flat_id-exclusion guarantee is exercised. Helpers `_assert_no_flat_id` (recursive) and `_assert_structured_resource_payload` assert: a `resource` object present with a non-empty `label` and a `type`, no `flat_id`/`flatId` key anywhere, no top-level `type`, no top-level `resourceId`. Merged the new model imports (`BatchOperationModel`, `GrantRequest`, `PermissionCheckRequest`, `ResourceObject`, `RevokeRequest`) into the existing top-level `authorization.models` import. Tested only through the public model interface. fix ✓. test: the 4 new tests pass (verified in isolation via `pants test ... -- -k TestMigrationProperty1StructuredIdentity` → succeeded). NOTE: the full-file run reports 8 PRE-EXISTING failures in the old flat-interface tests (`TestProperty1RequestConstruction`, `TestProperty3IdempotentSuccess`, `TestProperty4ValidationErrors`, `TestProperty6ImmediateFailure`) — all `AuthorizationClient.grant()/revoke() got an unexpected keyword argument 'resource_id'` because the client was migrated off the flat `resource_id` signature in tasks 2.3/2.4. Those tests are owned by task 2.7 (migrate client unit tests), not 2.5; not touched here. |
| 2.7 | pants_fix (file, x3), pants_test (file: test_client_grant.py, test_client_revoke.py, test_client_batch.py) | Migrated the three client unit-test files off the flat-shape API to structured `resource`. **test_client_grant.py**: all `grant(...)` calls now use `resource_label=` (+ `study`/`center` for parent-carrying types); added `_grant_response_body` factory; `test_grant_sends_post_with_structured_resource` asserts the request body carries `{userId, relation, resource:{type,label,study,center}}` with no top-level `type`/`resourceId` and no `flat_id`/`flatId`; `test_grant_treats_409_as_success_from_structured_resource` asserts the idempotent 409 result is built from the structured resource (`type=data_pipeline`, `resource_id=label`, `result.resource.{label,study,center}`) with no retry (one request); added `test_grant_rejects_invalid_identity_before_request` (empty label → `ValidationError`, zero requests sent). Used `data_pipeline` (study+center) for parent cases; kept `study`/`research_center` org-type cases with bare labels (no parents, valid). **test_client_revoke.py**: mirror changes — `_revoke_response_body` factory, structured DELETE body assertion, `test_revoke_treats_404_as_success_from_structured_resource` (idempotent 404 from structured resource, no retry), `test_revoke_rejects_invalid_identity_before_request`. **test_client_batch.py**: `_make_operations` now builds `BatchOperation` with `resource_label`/`study`/`center` (no `resource_id`); replaced `test_request_body_uses_camel_case_aliases` with `test_request_body_carries_structured_resource` asserting nested `resource` shape and no op-level `type`/`resourceId`; per-op `conflict`/`not_found` idempotency-exclusion and other-code-as-failure tests retained (already correct). **Issue found & fixed**: the batch path serializes via `model_dump_json(by_alias=True)` (not `request_dump()`), so `flatId` appears as `null` in batch op resources; adjusted the batch assertion to `resource.get("flatId") is None` (no *populated* flat identity) rather than key-absent, matching the actual public-interface output. fix ✓ (all three files). test ✓ for all three target files — every test in test_client_grant.py, test_client_revoke.py, test_client_batch.py passes. Did NOT run a full quality check (test task). **Pre-existing out-of-scope failures**: 8 failures remain in `test_property_grant_revoke.py` (`TestProperty1/3/4/6RequestConstruction/IdempotentSuccess/ValidationErrors/ImmediateFailure`) — these are the old flat-interface property tests still passing `resource_id=` to `grant()`/`revoke()`; that file belongs to task 2.5, not to 2.7's three named files, so it was left untouched. |
| Group 2 checkpoint | pants_fix (files), pants_test (dir: common/test/python/authorization_test), pants_lint (dir), pants_check (dir: common/src/python/authorization) | Fixed two defects that surfaced during the wave so the authorization suite passes under the structured-`resource` contract. **Defect 1 (batch serialization emitted flat_id/null fields):** `_execute_batch_chunk` in `client.py` built the body via `BatchRequestModel.model_dump_json(by_alias=True)`, which emitted `flatId: null` and null parents per operation (violates Req 1.3). Rewrote it to build the payload as plain dicts — `{operations: [{action, userId, relation, resource: op.resource.request_dump()}]}` — mirroring `GrantRequest.request_body()`, so each op's resource excludes `flat_id` and null fields. Order, chunk boundaries, `_BATCH_CHUNK_SIZE`, `retry_on_503`, `_IDEMPOTENT_ERROR_CODES`, and per-op classification unchanged. Removed the now-unused `BatchRequestModel` import. Left set_parents/update_resource/user-profile `model_dump_json` call sites untouched (out of scope). **Defect 2 (stale flat-interface tests):** Migrated the four stale property classes in `test_property_grant_revoke.py` (`TestProperty1RequestConstruction`, `TestProperty3IdempotentSuccess`, `TestProperty4ValidationErrors`, `TestProperty6ImmediateFailure`) to the structured signature via a new `grant_call_kwargs()` composite (type + non-empty label + permitted per-type parents); assertions now check the structured `resource` payload (no top-level `type`/`resourceId`, no `flat_id`) and idempotent results built from the structured resource. Also reconciled the same defect class in adjacent suite files that still used the removed `resource_id=`: `test_factory.py` (2 grant calls → `resource_label`), `test_logging.py` (9 grant/revoke calls → `resource_label`), `test_property_batch.py` (`_BATCH_OPERATIONS` strategy → `_batch_operations()` composite producing valid per-type parents; order-preservation assertions now read `sent["resource"]` structured payload), `test_property_models.py` (parse-error grant/revoke/batch calls → `resource_label`; **removed** the obsolete `TestResourceObjectValidation` class that tested the deleted `_ResourceObjectValidatorMixin` combined `{type}:{resourceId}` length rule — superseded by `TestProperty7ParentFieldCombinations` for per-type validation and the structured-identity request property for the label rule; Ruff auto-removed the now-unused imports), and `test_client_new_methods.py::TestCheckPermission` (5 `check_permission` calls for `data_pipeline` given the required `study` parent; `test_sends_post_to_check` now asserts the structured `resource` payload instead of top-level `type`/`resourceId`). **Results:** pants_test (authorization_test dir) → all 1880 tests pass, 20 skipped, 0 failed. pants_lint (dir) ✓. pants_check (common/src/python/authorization) ✓. No pre-existing failures in unrelated files were observed during the scoped runs. |
| 4.1 | pants_fix (file: common/src/python/authorization_sync/models.py), pants_check (dir: common/src/python/authorization_sync) | Reworked `DesiredGrant` in `authorization_sync/models.py` to carry the Structured Identity in place of the flat `resource_id`. Kept `@dataclass(frozen=True)` (hashable for set diff). Fields are now `user_id`, `resource_type`, `relation`, `resource_label`, and optional `center`/`study`/`community` (all `str \| None = None`) — exactly the Structured Identity Tuple components plus `user_id`; removed `resource_id`. Added `__post_init__` raising `ValueError("DesiredGrant requires resource_type and resource_label")` when `resource_type` or `resource_label` is missing/empty (Req 3.2). Added `identity()` returning `(resource_type, relation, resource_label, center, study, community)` (the diff key). Added `to_resource()` building a `ResourceObject(type, label, center, study, community)` (no flat_id). Rewrote `to_batch_op(action)` to build a structured `BatchOperation` from `action`/`user_id`/`resource_type`/`resource_label`/`relation`/`center`/`study`/`community` (dropped the old flat `resource_id`). Added `ResourceObject` to the `authorization.models` import. In-place edit; reused the existing dataclass. fix ✓. check: models.py clean; 2 EXPECTED errors remain — `translator.py:102` and `sync_service.py:119`, both `[call-arg] Unexpected keyword argument "resource_id" for "DesiredGrant"` — these are the old flat-`resource_id` call sites owned by later tasks (4.2, 6.x) and are NOT fixed here per the task instructions. No unrelated pre-existing errors surfaced in the authorization_sync directory. |
| 4.3 | pants_fix (file: common/src/python/authorization_sync/resource_ids.py), pants_check (dir: common/src/python/authorization_sync) | Tightened `build_label_for_resource_prefix` in `authorization_sync/resource_ids.py` to reject unknown prefixes (Req 3.7) while keeping the three known mappings unchanged (Req 3.6). Replaced the silent `return resource_name` fallback with `if prefix_label is None: raise ValueError(f"Unsupported resource prefix: {resource_prefix!r}")`, returning `f"{prefix_label}-{resource_name}"` for `datatype`→`ingest`, `dashboard`→`dashboard`, `page`→`page`. Updated the docstring: removed the `other -> "{name}"` line and added a `Raises: ValueError` note. `build_resource_id` left UNCHANGED (retained for seed path, Req 10.5 — out of scope). In-place edit; no new modules. fix ✓ (reflowed the `raise` onto one line, within 88 chars). check: resource_ids.py type-checks cleanly; 2 errors remain — `translator.py:102` and `sync_service.py:119`, both `[call-arg] Unexpected keyword argument "resource_id" for "DesiredGrant"` — these are from task 4.1's `DesiredGrant` change (owned by task 4.2), NOT from this edit; left untouched per task instructions. Since translator.py only calls this function with the three known prefixes, tightening does not change produced labels. |
| 4.2 | pants_fix (file: common/src/python/authorization_sync/translator.py), pants_check (dir: common/src/python/authorization_sync) | Populated structured fields in `translate()` in `authorization_sync/translator.py`. Removed the `build_resource_id` import and its call on the grant path (kept the `build_label_for_resource_prefix` import/call). Each `DesiredGrant` is now constructed with `resource_label=label`, `resource_type=api_resource_type`, `relation=relation`, `center=center_group_id` (None for general scope), `study=study_id` (from `StudyAuthorizations` else None), and `community=None` (grant path never targets community); inapplicable parents left None with no placeholders, no per-type branching added. Updated the module docstring (grant vocabulary now "resource_type, relation, and the structured resource label plus parent fields" instead of "resource_id") and the `translate()` docstring (replaced the flat ADR-016 "{center}_{label}-{study_id}" resource-id description with a Structured Identity description noting no flat id is built here). `pants_fix` reordered imports (moved `authorization_sync.*` imports above `users.authorizations` per isort grouping). Left `ACTIVITY_RELATION_MAP`, `validate_activity_relation_map`, `check_assignable` unchanged. In-place edit; no new modules. fix ✓. check: translator.py now type-checks cleanly (the previous `translator.py:102` call-arg error is resolved). 1 error remains — `sync_service.py:119` `[call-arg] Unexpected keyword argument "resource_id" for "DesiredGrant"` — owned by task 6.1, NOT fixed here per task instructions. No unrelated pre-existing errors surfaced in the authorization_sync directory. |
| 4.5 | pants_fix (file: common/test/python/authorization_sync_test/test_translator.py), pants_test (file) | Rewrote the translator unit tests off the flat-`resource_id` shape to assert the structured `DesiredGrant` (label + parents) and the `identity()` tuple. Removed the `_expected_resource_id` mirror of `_build_resource_id` (no study suffix, no center underscore-prefix) and the now-unused `Resource` import; replaced it with `_expected_label(prefix, name)` and a `PREFIX_TO_KIND` table producing only the structured label. Updated the module docstring's Validates references to 3.2, 3.3, 3.4, 3.6, 3.7. **`TestActivityToRelationMappingCorrectness`**: expected `DesiredGrant`s now built with `resource_label`, `resource_type`, `relation`, `center=None`, `study=study_id` (from `StudyAuthorizations`, else None on bare `Authorizations`), `community=None` — no composed flat id. **Replaced `TestCenterScopedResourceIds`** with `TestStructuredParentFields`: for center-scoped datatype/dashboard/page grants asserts `grant.center == center_group_id`, `grant.study == study_id`, `grant.resource_label == f"ingest-{datatype}"`/`dashboard-{name}`/`page-{name}`, `grant.community is None`, and `grant.identity()` returns the expected 6-tuple; added `test_general_scope_leaves_center_and_study_unset` (Req 3.4, inapplicable parents left None). **Added `TestResourcePrefixLabelMapping`**: parametrized prefix→kind table `datatype`→`ingest`, `dashboard`→`dashboard`, `page`→`page` (Req 3.6), a hyphenated-name preservation property, and `test_unknown_prefix_is_rejected` asserting `build_label_for_resource_prefix` raises `ValueError` for unsupported prefixes incl. `""` (Req 3.7). **Added `TestDesiredGrantConstructionRejection`**: empty `resource_label` and empty `resource_type` each raise `ValueError` (Req 3.2). All assertions go through the public interface (`translate`, `DesiredGrant`, `build_label_for_resource_prefix`); reused conftest strategies (`registry_ids_st`, `study_ids_st`, `center_group_ids_st`, `dashboard_names_st`, `page_names_st`, `valid_datatypes_st`, `mapped_activities_st`). fix ✓ (no changes needed). test ✓ — all 1894 tests pass. Did NOT run a full quality check (test task). NOTE: `conftest.py` still defines a stale `desired_grants_st`/`resource_ids_st` referencing `resource_id=` (used by other suites, not by this file); left untouched as out of scope for 4.5. |
| 5.1 | pants_fix (file: models.py, client.py), pants_check (dir: common/src/python/authorization) | Read identity solely from structured `resource` in the five response models. Removed top-level identity fields: `type`+`resource_id` from `GrantResult`/`RevokeResult`; `resource_id` from `PermissionEntry`; `type`+`resource_id` from `ResourceParents` (kept `parents`); `resource_id` from `ResourceListItem` (kept `structural_relation`/`display_name`). Set `model_config = ConfigDict(populate_by_name=True, extra="ignore")` on all five so an incoming top-level `resourceId`/`type` is silently dropped ("retain only as ignored input"; cannot fall back). Documented in docstrings: `resource is None` = no Structured Identity (no partial reconstruction); `flat_id` opaque, echoed byte-for-byte as GET `{resourceId}` path segment. Updated the two coupled client.py idempotent constructions (409-grant `GrantResult`, 404-revoke `RevokeResult`) to build solely from `resource=resource`, dropping `type=`/`resource_id=` kwargs. Did NOT touch `UserPermissions.to_grants` (task 5.2). fix ✓. check: 1 error — `models.py:383 PermissionEntry has no attribute resource_id` inside `UserPermissions.to_grants`, the exact 5.2-scoped code I was told not to change (expected transient coupling, resolved by task 5.2). No other errors in the authorization directory. Tests that ASSERT on `result.type`/`result.resource_id` to be updated in task 5.4 (noted, not fixed). |
| 5.2 | pants_fix (file: common/src/python/authorization/models.py), pants_check (dir: common/src/python/authorization) | Updated `UserPermissions.to_grants` factory contract in `authorization/models.py` to the structured-resource shape. Added module-level `import logging` and `log = logging.getLogger(__name__)`. Changed the factory annotation from `Callable[[str, str, str, str], Any]` (old `(user_id, resource_type, resource_id, relation)`) to `Callable[[str, "ResourceObject", str], Any]` for the new `(user_id, resource, relation)` signature. Body now iterates `self.permissions.items()`, and for each entry: skips it with `log.warning("Permissions entry for type %s has no resource; skipping (catalog gap)", resource_type)` and `continue` when `entry.resource is None` (Req 4.3, 6.1, 6.4 — catalog gap contributes no grant, conversion still completes); otherwise calls `factory(self.user_id, entry.resource, entry.relation)`. The `resource_type` key is no longer passed to the factory (available as `entry.resource.type`); `entry.resource_id` reference (removed field, the prior type error) is gone. Updated the docstring to describe the new (user_id, resource, relation) contract and the None-skip behavior. In-place edit; no other models touched. fix ✓. check: authorization directory now type-checks cleanly — the previous `PermissionEntry has no attribute resource_id` error inside `to_grants` (transient coupling left by task 5.1) is resolved. Consumers of `to_grants` (sync_service.py) are migrated in task 6.1; a related error there is expected until then and was not fixed. No unrelated pre-existing errors surfaced in the authorization directory. |
| 5.4 | pants_fix (files: test_client_query.py, test_property_models.py), pants_test (files) | Migrated the two response-parsing test files off the flat top-level `resourceId`/`type` shape to structured `resource`. **test_client_query.py**: `test_returns_user_permissions_on_200` response bodies now carry `{"resource": {"type": ..., "label": ...}, "relation": ...}` (study → org type, no parents; research_center → org type, no parents), and assertions read `entry.resource.type`/`entry.resource.label` instead of the removed `entry.resource_id`. Added three new tests: `test_parses_resource_with_parent_fields` (a `data_pipeline` entry carrying `study`+`center` parses and exposes them via `entry.resource.study`/`.center` — respects the per-type parent rules); `test_absent_resource_yields_no_identity` (an entry with no `resource` field parses with `entry.resource is None`, no failure); `test_top_level_resource_id_is_ignored_no_fallback` (a stray top-level `resourceId` in the body is dropped by `extra="ignore"` — `entry.resource is None`, `not hasattr(entry, "resource_id")` — proving no fallback to a flat id). **test_property_models.py**: added a `resource_objects()` composite strategy that draws a resource type and a permitted (study, center, community) presence combination per the design's per-type table (org types → no parents; data_pipeline/dashboard/page → table combinations) plus a non-empty label and an optional server-owned `flat_id`, so every generated `ResourceObject` passes the per-type validator and round-trips exercise `flat_id`. Rebuilt `grant_results()`/`revoke_results()` to construct `resource=draw(resource_objects())` (dropped the removed `type=`/`resource_id=` kwargs); `permission_entries()` now builds `resource=draw(resource_objects())` (dropped `resource_id=`); `resource_parents()` now builds `resource=draw(resource_objects())` + `parents=...` (dropped the removed top-level `type=`/`resource_id=`). Round-trip tests in `TestProperty2ResponseRoundTrip` unchanged (they compare via model equality, so serialize→parse→equal still holds); parse-error properties in `TestProperty10` unchanged and still meaningful. Coverage not weakened — added 3 targeted client parse tests and kept all property round-trip/parse-error coverage. All assertions go through the public model interface. fix ✓ (both files, no changes needed). test ✓ — both target files pass (full suite reports all tests passed, 0 failed). Did NOT run a full quality check (test task per steering). No pre-existing failures surfaced in the scoped runs. |
| 6.1 | pants_fix (file: common/src/python/authorization_sync/sync_service.py), pants_check (dir: common/src/python/authorization_sync) | Built `DesiredGrant` from the structured `entry.resource` in both diff-input paths of `sync_service.py`, completing the migration off the flat `resource_id`. Added `ResourceObject` to the `authorization.models` import. Added a module-level adapter `_grant_from_resource(user_id, resource, relation) -> DesiredGrant` that maps the new `UserPermissions.to_grants` factory contract `(user_id, resource, relation)` (task 5.2) to `DesiredGrant`'s keyword fields, reading `resource_type=resource.type` (structured, authoritative), `resource_label=resource.label`, and `center`/`study`/`community` from the structured resource — never a flat id. Changed the `sync_users` current-set build from `permissions.to_grants(DesiredGrant)` (bare class, incompatible with the new factory signature and the removed `resource_id`) to `permissions.to_grants(_grant_from_resource)`. In `_grants_in_scope`, replaced the old `DesiredGrant(..., resource_id=entry.resource_id, ...)` construction with one built from `entry.resource` (`resource_label=r.label`, `center=r.center`, `study=r.study`, `community=r.community`); added an explicit `r = entry.resource; if r is None: continue` guard after the `_entry_in_scope` skip — `_entry_in_scope` already returns False on `resource is None`, so this is a defensive no-op that also narrows the `ResourceObject \| None` type for mypy across the function boundary. Left `_entry_in_scope` (task 6.2), `grants_to_add = desired - current`, `grants_to_revoke = in_scope_current - desired`, the per-type query loop, and event reporting unchanged; the tuple key follows from `DesiredGrant` equality. fix ✓. check ✓ — the `common/src/python/authorization_sync` directory now type-checks cleanly (the previously-remaining `sync_service.py` `Unexpected keyword argument "resource_id"` error, owned by this task, is resolved). No unrelated pre-existing errors surfaced. Source task: did NOT run a full quality check per steering. |
| 6.2 | None (no-op verification, no edits) | Verified `_entry_in_scope` and `sync_users` in `authorization_sync/sync_service.py` already satisfy all listed requirements — NO code changes made. `_entry_in_scope` already: (1) returns `False` when `entry.resource is None` (scope cannot be confirmed → never revoked, Req 6.1/6.2/6.3/7.5); (2) returns `False` when `entry.resource.community is not None` (community-scoped out of scope, Req 7.3); (3) returns `entry.resource.center == center_group_id`, a Python `==` string comparison which is exact/case-sensitive, so a non-matching center yields `False` (Req 7.1/7.2). Center/community are read directly from the structured `entry.resource` — no flat-string parsing anywhere (Req 6.4/7.4). The docstring already accurately documents the case-sensitive center comparison and the None/community exclusions (states scope is "read from the structured `resource` … never parsed out of the flat resource id", community "is a different scope even if it also names a center", and `resource` None → "treated as out of scope so it is never revoked"). `sync_users` preserves: the per-type query loop (`for resource_type in _SYNC_RESOURCE_TYPES: get_user_permissions(type_filter=resource_type)`, one request per type, Req 8.5/8.6); batch apply (`self._client.batch(operations)`, Req 9.6); and partial-failure event reporting (`if result.failed > 0: self._report_partial_failure(...)`, Req 9.5). Per task-execution steering, skipped Pants checks since this was a pure no-op verification with zero edits. |
| conftest (prereq for 6.3–6.6) | pants_fix (file: common/test/python/authorization_sync_test/conftest.py) | Migrated the shared `authorization_sync_test/conftest.py` Hypothesis strategies off the removed flat `resource_id` to the structured shape. Imported `ResourceObject` from `authorization.models`. **Removed** the stale `resource_ids_st` (flat `{center}_{label}-{study}` builder). **Added** `community_ids_st` (non-empty ids) and `flat_ids_st` (arbitrary server-owned opaque handles that deliberately follow no client-reproducible format). **Added** `resource_labels_st` (`ingest-{name}`/`dashboard-{name}`/`page-{name}`). **Added** `_parent_fields_for_type(resource_type)` returning a strategy of `{study, center, community}` dicts valid per the per-type table (data_pipeline: study+center or study; dashboard: +community-alone; page: +center-alone/community-alone), so any `ResourceObject` built from the output passes `check_parent_fields`. **Replaced** `desired_grants_st` (was `st.builds(DesiredGrant, resource_id=...)`) with a `@st.composite` that draws a type, a valid parent combo, a label, relation, and user id and builds a structured `DesiredGrant` (no `resource_id`); `desired_grant_sets_st` now uses `desired_grants_st()`. **Added** `resource_objects_st(resource_type=None, with_flat_id=False)` composite (valid per-type `ResourceObject`, optional opaque `flat_id`). **Rewrote** `permission_entries_st(resource_type=None, allow_none_resource=False, with_flat_id=False)` to build `PermissionEntry(resource=ResourceObject(...) | None, relation=..., access=..., inherited_from=None)` — supports generating an optional `flat_id` on the resource and, when `allow_none_resource`, `resource=None` entries. Updated `user_permissions_st` to pass each group's `resource_type` to `permission_entries_st` so entry resources match their group key (as the real API returns). Left all non-stale strategies, `MockAuthorizationClient`, and the `mock_authorization_client`/`mock_event_collector` fixtures unchanged. fix ✓. Validated by the directory-scope `pants_test` below (all sync suites, incl. already-migrated `test_translator.py`, pass with the migrated conftest). |
| 6.3, 6.4, 6.5, 6.6 | pants_fix (file: test_multi_study_sync.py), pants_test (dir: common/test/python/authorization_sync_test) | Rewrote `test_multi_study_sync.py` onto the Structured Identity Tuple. **Migrated `StatefulAuthorizationClient`** off flat `resource_id`: grants are keyed on `GrantKey = (user_id, resource_type, resource_label, relation, center, study, community)`; `get_user_permissions` returns `PermissionEntry(resource=ResourceObject(...))` (or `resource=None` for keys in `scopeless_keys`, optional `flat_id` from the `flat_ids` map); `batch` applies grant/revoke keyed on the structured tuple recovered from each `BatchOperation`. Migrated all 10 regression tests (helpers `_grant_key`, `_key_from_op`, `_revoke_ops`/`_grant_ops`) to structured identity; the previously-general parent-less page grants now carry a valid parent (study-only for the non-center page; community for the community-scoped page) since a `page` `ResourceObject` requires at least one parent. **6.3 (Property 2 — diff round-trips independent of flat_id):** `test_diff_round_trips_independent_of_flat_id` seeds the client's current state from a generated desired set, assigning arbitrary differing server-owned `flat_id`s, then drives the real `AuthorizationSyncService.sync_users` with the desired set injected via `patch("authorization_sync.sync_service.translate", return_value=desired)`; asserts no grant and no revoke ops were issued (the service returns early with no changes) — matching by structured tuple regardless of flat_id. Uses a `_desired_sets_st` composite (uniform user id, independent center scope, grants from conftest `desired_grants_st()` so parent combos stay valid/constructible). **6.4 (Property 3 — None-resource never revoked):** `test_none_resource_entry_never_revoked` builds `resource=None` entries, asserts `_grants_in_scope` returns empty without raising (catalog-gap path), and that driving `sync_users` (empty desired, stubbed `get_user_permissions`) yields no grant/revoke ops. **6.5 (Property 5 — community never revoked):** `test_community_scoped_entries_never_revoked` forces a non-null `community` on page entries and asserts `_grants_in_scope` excludes them all (empty) regardless of the reconciled center. **6.6 (Property 6 — revoke candidacy center-keyed):** `test_revoke_candidacy_is_center_keyed` generates page entries with a resource and no community, and asserts an entry is in `_grants_in_scope` iff its `resource.center` equals (case-sensitive) the reconciled center group id. All four `@settings(max_examples=100)`, tagged with the design property comment and a `Validates:` line; test only through the public interface (`sync_users`, `_grants_in_scope`) reusing conftest strategies. Not PBT-status-tracked tasks. **Fixes during work:** (1) import must be `from .conftest import ...` (relative) not `from conftest import ...` — the latter caused a collection ImportError in the pants sandbox; (2) the migrated general page regression grant needed a `study` parent to satisfy the `page` per-type validator. fix ✓. **Directory-scope pants_test result: all 1901 tests passed, 20 skipped, 0 failed** — the whole `authorization_sync_test` suite (test_translator.py, test_fault_isolation.py, test_pipeline_integration.py, test_sync_profile.py, test_validator.py, test_multi_study_sync.py) validated against the migrated conftest. No failures surfaced in the task-6.7-owned files (test_fault_isolation.py, test_pipeline_integration.py, test_sync_profile.py). Confirmed all 4 new property tests + 10 regression tests collected via the JUnit report. Did NOT run a full quality check (test task per steering). |
| 6.7 | pants_fix (file: test_fault_isolation.py), pants_test (dir: common/test/python/authorization_sync_test) | Re-keyed / strengthened the three sync-service diff test files onto the Structured Identity shape. **Audit first:** grep confirmed NO flat `resource_id` assertions or `resource_id=` constructions remain in any of the three files (the only `resource_id`-ish token in the directory is the `authorization_sync.resource_ids` module import in `test_translator.py`, out of scope) — the conftest migration had already moved these files to the structured shape (DesiredGrant with `resource_label`+parents, PermissionEntry/ResourceObject structured, BatchOperation structured), so there was nothing to re-key. Coverage gaps closed instead. **test_fault_isolation.py (changed):** added imports for `_SYNC_RESOURCE_TYPES`, `ACTIVITY_RELATION_MAP`, and `ServiceUnavailableError`. Added `TestPerTypeQuerying` (2 Hypothesis tests, Req 8.5/8.6): `test_one_permissions_request_per_resource_type` asserts `sync_user` issues exactly one `get_user_permissions` per synced type (queried type set == `_SYNC_RESOURCE_TYPES`, count == len, no type queried twice/skipped) via the conftest `MockAuthorizationClient.get_user_permissions_calls`; `test_every_permissions_request_carries_a_type_filter` asserts every request carries a non-empty `type_filter` in `_SYNC_RESOURCE_TYPES` and the right user_id (Req 8.6 "never issue a request without a type"). Added `TestPreservedBehaviorHeldConstant` (3 tests, Req 9.1/9.2/9.3): `test_sync_resource_types_derived_from_activity_relation_map` pins `_SYNC_RESOURCE_TYPES == frozenset(types derived from ACTIVITY_RELATION_MAP values)` (9.1); `test_activity_relation_map_association_is_constant` pins the exact key set, value set, and each ordered key→value association of `ACTIVITY_RELATION_MAP` against a literal snapshot (9.2); `test_service_unavailable_after_retries_is_caught_not_propagated` drives `sync_user` with a `MockAuthorizationClient` raising `ServiceUnavailableError` (503-retry-exhausted surface) and asserts it is caught and reported via the collector without propagating (9.3). Existing `test_partial_batch_failure_reports_individual_error_events` already covers 9.5 (left as-is). **test_pipeline_integration.py (unchanged):** tests process→service wiring (8.1/8.2/8.4/8.5) with the sync service fully mocked; `test_sync_called_after_visitor_with_all_studies_together` already confirms the one-`sync_users`-call-with-all-studies wiring (the precondition for the service's one-request-per-type behavior) and per-study `sync_user` is not used. No flat-id assertions; nothing to re-key; not weakened. **test_sync_profile.py (unchanged):** tests `sync_profile` only (3.4/3.5/6.3) — no diff/query/BatchOperation/DesiredGrant/PermissionEntry, so nothing to re-key. Req 9.6 reconciliation semantics are covered exhaustively in `test_multi_study_sync.py` (not one of the three files) — not duplicated here. All new assertions go through the public interface (`sync_user`, the module-level `_SYNC_RESOURCE_TYPES`/`ACTIVITY_RELATION_MAP` constants, the collector) and reuse conftest strategies (`registry_ids_st`, `authorizations_st`, `center_group_ids_st`) and the conftest `MockAuthorizationClient`. fix ✓. test ✓ — pants_test (authorization_sync_test dir) → all 1906 pass, 0 failed (1901 → 1906 = the 5 new tests). Verified the new classes execute via `-k TestPerTypeQuerying or TestPreservedBehaviorHeldConstant` (pass). Did NOT run a full quality check (test task per steering). No pre-existing failures surfaced in the scoped runs. |
| 7 (checkpoint) | full_quality_check (all) x3 | Checkpoint after sync-side migration (task groups 4/5/6). **Run 1:** fix ✓, lint ✓, check ✕ — 12 mypy errors, all in spec-modified test files still asserting on response-model attributes removed by task 5.1 (`type`/`resource_id` now live only under the structured `resource`). Fixed by reading identity from `result.resource`/`entry.resource`: `test_client_grant.py` (`_grant_response_body` now synthesizes a structured `resource`; two assertion blocks → `resource.type`/`.label`), `test_client_revoke.py` (same pattern, `_revoke_response_body` + two blocks), `test_client_parents.py` (response body carries `resource`, assert `resource.type`/`.label`), `test_client_new_methods.py` (`ResourceListItem` body carries structured `resource`, assert `resource.label`), and `test_multi_study_sync.py:609` (`authorizations=[object()]` → typed list, first attempt used `[]`). **Run 2:** fix ✓, lint ✓, check ✓, test ✕ — 2 failures, both from the Run-1 test edits: (a) `test_client_grant.py::test_grant_returns_grant_result_on_200` — the `dashboard` response `resource` I synthesized had no parent, failing the per-type validator; fixed by giving that case an explicit `resource` with `study` (matching the grant's `study="study-1"`). (b) `test_multi_study_sync.py::test_diff_round_trips_independent_of_flat_id` — my `authorizations=[]` fix made `sync_users`' `for authorization in authorizations:` loop skip the (stubbed) `translate`, leaving `desired` empty so every in-scope current grant was revoked; fixed by passing `[Authorizations()]` (one element so stubbed translate runs once; contents ignored). **Run 3:** fix ✓, lint ✓, check ✓ (603 files), test ✓ (all suites pass; authorization + authorization_sync + downstream user_management gear integration tests green). No downstream `user_management` consumer used the old flat-`resource_id` signatures — its integration tests (`test_general_authorization_integration.py`, `test_integration_gear_error_handling.py`) pass unchanged. No pre-existing unrelated failures observed. Files modified: test_client_grant.py, test_client_revoke.py, test_client_parents.py, test_client_new_methods.py, test_multi_study_sync.py (tests only; no source changes). |
| 8.1 | None (no-op verification; no files modified) | Confirmed `build_resource_id` retention and its removal from the grant path — a pure verification task with NO code changes. **(1)** `translator.py` has NO import or call of `build_resource_id` (grep of `common/src/python/authorization_sync/translator.py` → no matches; task 4.2 removed it). **(2)** `build_resource_id` is defined UNCHANGED in `common/src/python/authorization_sync/resource_ids.py` (center-scoped `"{center}_{label}-{study_id}"` / non-center `"{label}-{study_id}"` ADR-016 format) — retained per Req 10.5 because a live consumer remains. **(3)** The `authorization_sync/__init__.py` re-export is present — imported from `authorization_sync.resource_ids` and listed in `__all__` — kept as the seed path's public surface. **(4)** Live consumer confirmed: `common/src/python/projects/study_mapping.py` imports `build_resource_id` and calls it in `__auth_resource_id` (with `center_id`+`study_id`) and `__auth_resource_id_no_center` (with `study_id`) — unchanged. **(5)** `common/src/python/projects/hierarchy_seeder.py` still calls `self._client.set_resource_parents(resource_type=..., resource_id=resource_id, parents=...)`, passing a `{resourceId}` path segment (not a structured resource body) — seed path NOT migrated, left unchanged. Per task-execution steering, Pants checks skipped for a pure no-op verification. Requirements 10.4, 10.5 satisfied. |
| Task 9 (Final checkpoint) | full_quality_check (all): fix ✓, lint ✓, check ✓, test ✓ | Ran `full_quality_check` (fix → lint → check → test on `::`) one final time to confirm the whole tree is green after the completed migration. **fix**: exit 0 — ruff format, ruff check --fix, docformatter, ruff all made no changes. **lint**: exit 0 — ruff check, ruff format, docformatter, hadolint all passed ("All checks passed!"). **check**: exit 0 — mypy succeeded, no issues found in 603 source files (memoized). **test**: exit 0 — all pytest targets succeeded (every target reported `succeeded`, 0 failed; results memoized from the green state established at checkpoint 7). Coverage report generated; TOTAL 84% (41677 stmts, 6643 miss); migrated modules healthy — `authorization/models.py` 99%, `authorization/client.py` 80%, `authorization_sync/models.py` 95%, `authorization_sync/resource_ids.py` 100%, `authorization_sync/sync_service.py` 86%, `authorization_sync/translator.py` 95%. No failures in spec-modified code (authorization/, authorization_sync/, their tests) or downstream consumers; no unrelated pre-existing failures surfaced. Migration complete and the codebase is green. |
