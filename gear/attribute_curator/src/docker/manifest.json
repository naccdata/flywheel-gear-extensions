{
    "name": "attribute-curator",
    "label": "Attribute Curator",
    "description": "Curation of derived data for MQT project",
    "version": "1.2.2",
    "author": "NACC",
    "maintainer": "NACC <nacchelp@uw.edu>",
    "cite": "",
    "license": "MPL-2.0",
    "url": "https://naccdata.github.io/flywheel-gear-extensions",
    "source": "https://github.com/naccdata/flywheel-gear-extensions",
    "environment": {
        "FLYWHEEL": "/flywheel/v0"
    },
    "custom": {
        "gear-builder": {
            "category": "analysis",
            "image": "naccdata/attribute-curator:1.2.2"
        },
        "flywheel": {
            "suite": "Curation",
            "show-job": true
        }
    },
    "inputs": {
        "api-key": {
            "base": "api-key",
            "read-only": false
        },
        "rxclass_concepts_file": {
            "description": "JSON file containing cached RxClass concepts. If provided, skips the RxClass querying at the beginning of curation and uses the data provided here",
            "base": "file",
            "optional": true,
            "type": {
                "enum": [
                    "source code"
                ]
            }
        }
    },
    "config": {
        "dry_run": {
            "description": "Whether to do a dry run",
            "type": "boolean",
            "default": false
        },
        "filename_patterns": {
            "description": "Filename patterns to match on; comma-deliminated for multiple patterns",
            "type": "string",
            "default": ".*\\.json,.*\\.dicom\\.zip,.*\\.nii\\.gz"
        },
        "include_subjects": {
            "description": "Comma-deliminated list of subjects to include",
            "type": "string",
            "default": ""
        },
        "exclude_subjects": {
            "description": "Comma-deliminated list of subjects to exclude",
            "type": "string",
            "default": ""
        },
        "curation_tag": {
            "description": "Tag to apply to curated files",
            "type": "string",
            "default": "attribute-curator"
        },
        "force_curate": {
            "description": "Curate file even if it's already been curated",
            "type": "boolean",
            "default": true
        },
        "ignore_qc": {
            "description": "Curate file even if QC failed",
            "type": "boolean",
            "default": false
        },
        "max_num_workers": {
            "description": "Max number of workers to use",
            "type": "integer",
            "default": 4
        },
        "debug": {
            "description": "Whether or not to use debug-level logging (level set to DEBUG)",
            "type": "boolean",
            "default": false
        }
    },
    "command": "/bin/run"
}